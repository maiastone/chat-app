const assert = require("chai").assert;

describe("sanity check for our test bundle", function () {
  it("should work", function () {
    assert(true);
  }); //end of work
}); //end of describe sanity check for our test bundle
 
